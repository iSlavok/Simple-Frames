#This file pins the Gradle daemon JVM. Minecraft 26+ (unobfuscated) needs JDK 25,
#and the 26.x build nodes are only registered when the daemon runs on JDK >=25, so
#pin the daemon to 25 for a consistent set of versions (and the ability to build 26.x).
toolchainVersion=25

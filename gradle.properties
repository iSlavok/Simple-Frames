# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod Properties
mod_version=1.0.0
maven_group=online.slavok.frames
archives_base_name=simple-frames

# Modrinth project (slug or id) — set once the project exists; used by the release pipeline.
# modrinth_id=

# Shared, version-independent dependency versions.
# Per-Minecraft-version deps (yarn, fabric-api, fabric-language-kotlin,
# fabric-permissions-api, java level) live in the build.gradle.kts matrix.
loader_version=0.18.1
